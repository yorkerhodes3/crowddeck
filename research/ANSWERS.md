=== ableton-link ===
Ableton Link is an open‑source, peer‑to‑peer library that shares a single global timeline—tempo, beat position and phase—across any number of applications on one or more devices on a local network; each participant creates a Link object, enables it, and optionally supplies a “quantum” value (in beats) that defines the unit of phase alignment so that all peers with the same quantum stay perfectly in sync and peers with multiples of each other’s quantum stay aligned at the larger boundaries (e.g., a 8‑beat loop aligns with a 4‑beat loop at the start of each 8‑beat bar). The SDK provides functions such as getTempo/setTempo, getBeat, getPhase(quantum), isPlaying, enableStartStopSync, requestBeatAtTime(beat,time,quantum) and forceBeatAtTime, which allow reading the current tempo and phase, changing tempo, and requesting a specific beat to occur at a precise time; when start/stop sync is enabled, transports are quantized to the next quantum boundary so that all participants launch or stop together, while the underlying clock runs continuously and each peer can independently compute its local beat and phase from the shared timeline.

=== audio-analysis-open-source ===
Aubio is a lightweight C library (with Python wrappers) that provides real‑time‑capable onset detection, beat tracking, BPM estimation and pitch tracking, and it also includes a basic key‑detection algorithm; it is released under the GNU GPL. Essentia is an extensive open‑source C++ framework (with Python bindings) offering more than 250 algorithms, including robust beat‑tracking and tempo (BPM) extraction as well as high‑level tonal descriptors such as chroma, pitch‑salience and a dedicated key‑detection extractor; it is distributed under the AGPL‑v3 license. Librosa is a pure‑Python library widely used in music‑information‑retrieval research that supplies onset envelopes, autocorrelation‑based tempo estimation and beat tracking, and it can derive musical key by computing chroma features and applying standard key‑profile correlation (though it does not ship a dedicated key‑finder module); it is released under a BSD‑style license. QM‑DSP KeyFinder is a standalone open‑source key‑detection algorithm (originally written in C++ and used in DJ software such as Mixxx) that analyzes a long‑term chroma histogram and matches it to key profiles, offering higher accuracy for tonal music than many generic approaches and being available under an MIT‑compatible licence. All four tools are free to use, but they differ in language bindings, licensing, algorithmic depth and typical use cases: Aubio for quick, low‑latency analysis; Essentia for comprehensive, high‑performance MIR pipelines; Librosa for research prototyping in Python; and QM‑DSP KeyFinder for precise standalone key detection.

=== beets-musicbrainz ===
Beets is a command‑line music library manager that automatically improves tag information by querying the MusicBrainz database, and it can also generate and use acoustic fingerprints via the Chromaprint/AcoustID plugin (called “chroma”). When the chroma plugin is enabled together with the musicbrainz plugin, beets fingerprints each imported file with the external fpcalc tool, stores the resulting acoustid_id and acoustid_fingerprint fields, and sends the fingerprint to the AcoustID web service; the returned AcoustID match is then resolved through MusicBrainz to supply album, track and artist candidates for autotagging even when the original ID3 tags are missing or incorrect. The plugin’s auto option controls whether fingerprinting occurs during import, and a chromasearch command lets users query the local library for tracks with similar fingerprints to locate duplicates or alternate encodings. In addition to MusicBrainz‑based metadata such as album art, genres, and release information, beets can embed these acoustic fingerprint tags in files, making it a comprehensive solution for organizing and correcting large music collections.

=== crowd-request-features ===
Bar‑oriented jukebox platforms now combine QR‑code or browser‑based song request queues with real‑time crowd voting and built‑in anti‑abuse controls such as vote‑reset cooldown timers that limit how often a guest can cast new votes, preventing vote‑spamming and ensuring a fair rotation of tracks; hosts can also configure explicit‑content filters that automatically block songs, album art or videos marked as explicit, set genre or artist restrictions, and apply fallback playlists to keep music flowing when requests run dry, while still allowing full control over queue rules, manual overrides and revenue‑generating features like pay‑per‑song or tip models.

=== digital-jukebox-competitors ===
AMI Entertainment’s NEXTGEN line, exemplified by the NGX‑32 and NGX Curve models, distinguishes itself with interchangeable “Smart Skins” that physically wrap the unit and can trigger custom graphics and interactive features, a fully networked core that supports remote route‑management via the Co‑Pilot app, integrated music‑video playback of more than 35,000 videos, free‑to‑play trivia games, ad‑upload capabilities, and a swappable core design that simplifies maintenance; its competitors – Pandora CloudCover, NSM Entertainment Network, Rockbot, Edge226 and Soundtrack – focus primarily on cloud‑based music streaming and licensing platforms that deliver curated playlists, DJ‑style automation, digital signage or background‑music services, and in some cases revenue‑sharing ad models, but they generally lack the dedicated hardware‑centric features of NEXTGEN such as interchangeable skins, on‑site video jukebox capability, built‑in trivia, and the operator‑focused remote‑management suite that lets venues control volume, schedule content, run promotions and track detailed route reporting from a single mobile interface.

=== dj-controller-standards ===
The de‑facto standard for modern DJ controllers that support both MIDI and HID is to use HID mode for any jog‑wheel work that requires high resolution, because traditional 7‑bit MIDI only supplies 0‑127 steps (14‑bit MIDI exists but most CDJ‑style units don’t implement it) whereas HID can transmit many more increments per turn; in practice Pioneer CDJs and many Serato‑certified accessories expose a 128‑step base count per full rotation and mapping files typically multiply that base to achieve smooth scrolling – the most common “range” value seen in Serato XML mappings is 1536 (128 × 12), although other mappings use 450, 1500 or similar counts depending on the wheel’s firmware; to enable these HID messages in Serato or Traktor you must set the unit’s HID mode to “Advanced” (or “Standard” for basic functionality) in the utility menu, then edit the controller’s XML (Serato) or .tsk/.tmi (Traktor) mapping file, inserting the desired range value for the jog‑wheel element and assigning the appropriate relative‑turn messages, which lets the software interpret each HID packet as a fine‑grained position change rather than the coarse 7‑bit steps of pure MIDI.

=== dj-software-comparison-2025 ===
In the 2025 landscape the leading DJ‑software choices can be differentiated by platform coverage, pricing model, hardware integration, and flagship features: Serato DJ Pro (Windows & macOS) retails for roughly $179 perpetual (or $129 / yr) and is prized for industry‑standard DVS, exceptionally responsive scratching, extensive plug‑and‑play support for virtually any USB controller, and recent addition of key‑shift and AI‑driven track‑analysis, though it lacks a native video engine; Rekordbox 7 (Windows & macOS) costs about $129 perpetual and offers the tightest integration with Pioneer CDJ/XDJ gear, a powerful preparation workflow, built‑in stems separation, cloud‑sync for playlists, but its video capabilities are limited and its performance UI is less customizable; Traktor Pro 4 (Windows & macOS) is priced around $119 perpetual, shines with modular controller mapping, a rich suite of FX and remix decks, robust beat‑grid editing, and strong community presets, yet it provides no native video mixing and relies on third‑party tools for CDJ connectivity; djay Pro AI (Windows, macOS, iOS, iPadOS) follows a subscription model (~$19 / month) and distinguishes itself with AI‑driven stem separation, seamless Spotify/Apple Music integration, cloud‑based library sync, and an intuitive touch‑optimized UI, though its effects library is narrower than the pro‑tier competitors; VirtualDJ 2025 (Windows & macOS) offers a free Lite tier and a full‑feature Pro license (~$299) that includes comprehensive video mixing, VST plug‑in support, karaoke, and one‑click mapping for thousands of controllers, making it the most versatile but also the most feature‑dense and price‑heavy option; finally, Mixxx (Windows, macOS, Linux) is 100 % free, open‑source, and supports virtually any MIDI controller with fully editable mappings, provides basic stem analysis, and includes essential DJ tools such as sync and cue, though its UI is less polished, it lacks official cloud or AI features, and updates depend on community contributions.

=== djay-pro-features ===
djay Pro AI includes Algoriddim’s Neural Mix™ technology that separates tracks into four real‑time stems—vocals, harmonics, drums and bass—allowing full control of each source on any deck; an AI‑enhanced Automix (Automix AI) that automatically detects intros, outros and rhythmic patterns to create seamless, production‑style transitions; support for up to four decks with a dedicated four‑deck layout and crossfader‑fusion that can blend any combination of the isolated stems; a full suite of hardware integration features including MIDI mapping, pre‑cueing, DVS (AI‑driven digital vinyl control), and compatibility with major controllers and mixers; advanced audio tools such as a sampler, slicer, up to three chainable audio effects per deck, XY‑Pad, instant FX, and Sugar Bytes effects; a high‑precision Fluid Beatgrid for beat‑matching, beat‑grid editing, and pitch‑stretching; video mixing capabilities with visual effects, photos and titles; plus additional DJ utilities like looping, cue points, slip mode, waveform zoom, and a built‑in sequencer/looper for creating live beats and remixes.

=== djay-pro-hardware-streaming ===
djay Pro offers full hardware integration with more than 100 class‑compliant MIDI controllers, including native support for popular lines such as Pioneer DJ, Reloop (Mixtour Pro, Buddy, Mixon 8 Pro, RMX‑95, PTB‑2), Numark, Hercules, Denon DJ (Prime 4, Prime Go+, SC Live 2/4, Mixstream Pro +, Mixstream Pro Go), and AlphaTheta, and it features an advanced MIDI‑Learn system for custom mapping as well as Digital Vinyl System (DVS) compatibility. The software also streams directly from the major services TIDAL (Hi‑Fi audio and video streams with Neural Mix stems), SoundCloud (via SoundCloud Go+ for over 200 million tracks), and Beatport (including offline storage of 50–100 tracks per subscription), allowing DJs to mix and remix catalogues from these platforms without leaving the app.

=== free-music-apis ===
Jamendo offers a public Music API that lets developers search its catalog of over 500 000 Creative‑Commons‑licensed tracks, retrieve album and artist details, and obtain streaming‑ready URLs for download or embed; access requires a free client‑id obtained by registering an application and all standard read‑only calls are free, while commercial use of certain features may need a separate agreement. The Free Music Archive also provides a JSON‑based API for querying its library of CC‑licensed songs, exposing metadata such as title, artist, genre, and license type and allowing direct download links for tracks that are free for non‑commercial or commercial use depending on the specific license. ccMixter runs the open ccHost Query API, a publicly available REST interface that returns searchable results for tracks, samples, and remixes along with their Creative‑Commons license information, enabling developers to integrate its remix‑friendly catalog into apps, websites, or smart‑device platforms. All three services supply royalty‑free music under Creative Commons terms, with licensing details included in the API responses so users can respect attribution, non‑commercial, or share‑alike requirements as appropriate.

=== funkwhale-ampache-airsonic ===
Funkwhale is a federated, ActivityPub‑based music server that offers a modern web UI, robust tag and playlist handling, multi‑user support with role‑based access, and scrobbling to services like Last.fm and ListenBrainz, but it lacks native Subsonic API compatibility, on‑the‑fly transcoding and DLNA support, relying instead on optional third‑party Subsonic clients for mobile playback; Ampache is a long‑standing PHP‑driven solution with full Subsonic/OpenSubsonic API support, an integrated web player, ffmpeg‑based transcoding, extensive multi‑user ACLs, scrobbling (Last.fm, Libre.fm, ListenBrainz), podcast and internet‑radio feeds, cover‑art handling, smart playlists, rating, tag editing and optional DLNA plugins; Airsonic is a Java implementation that also provides the Subsonic API, a built‑in responsive web UI, real‑time transcoding, per‑user role management, podcast and radio integration, cover‑art, DLNA/UPnP streaming and a wide ecosystem of Subsonic clients; Airsonic‑Advanced is a fork of Airsonic that adds more granular access controls, enhanced transcoding options and optional paid support while retaining the same API compatibility, web interface and feature set (podcasts, radio, cover‑art, DLNA); Gonic is a lightweight Go‑based server offering the OpenSubsonic API, a simple web UI with playlists and starred items, ffmpeg transcoding, basic multi‑user accounts, scrobbling to Last.fm and ListenBrainz, but it omits DLNA, podcast handling and advanced role management, focusing instead on speed and low resource usage.

=== jukebox-licensing-us ===
The Jukebox License Office (JLO) was a joint‑venture clearinghouse created by the three major U.S. performing‑rights organizations—ASCAP, BMI and SESAC—that historically offered a single, economical annual license covering public performances of any song in all three repertories for qualifying coin‑operated jukeboxes (machines operated by patrons with coins or tokens, used solely for non‑dramatic music playback in venues that do not charge admission). To qualify, the jukebox must be a “coin‑operated phonorecord player” as defined by U.S. copyright law, and the license required the display of a certificate on the unit. While many operators previously obtained this unified license through the JLO, the JLO ceased operations in 2025; consequently, owners of commercial jukeboxes now must secure separate annual jukebox licenses directly from ASCAP, BMI and SESAC, each covering that organization’s catalog, and continue to ensure the jukebox meets the statutory criteria and displays any required proof of licensing.

=== jukebox-remote-request-apps ===
Popular alternatives to Rockbot for a bar‑style jukebox and crowd‑requested music include BeatTribe, which lets guests scan a QR code and submit songs in seconds without downloading an app; Jukestar, a social jukebox that integrates with Spotify and lets patrons request, vote and veto tracks; TouchTunes, a hardware‑linked app that offers a pay‑per‑song model and revenue sharing with venues; SongUp, a browser‑based queue that supports voting, limits and free or low‑cost plans; Festify and Spotify Jam, which require a host with a Spotify Premium account and let guests add songs to a shared playlist; AmpMe, which syncs playback across multiple phones for groups without dedicated speakers; Apple Music SharePlay for all‑Apple environments; Soundsuit, a business‑focused streaming service with curated playlists and licensing built in; and traditional background‑music services like Soundtrack Your Brand that provide licensed music streams with limited guest interaction. All of these options let bar owners choose between full‑service jukebox hardware, mobile‑only request platforms, or hybrid solutions that balance control with audience participation.

=== karaoke-open-source ===
Karaoke Eternal is a self‑hosted, browser‑based karaoke party system that plays MP3+G (CDG) files and MP4 videos, offers QR‑code joining, multiple password‑protected rooms, dynamic fair queues, music‑synced visualizations and runs without ads or telemetry; OpenKJ is a cross‑platform open‑source karaoke‑show host built with Qt and GStreamer that handles MP3/OGG/WAV paired with CDG (including zip bundles), provides key‑changing, tempo control, EQ, end‑of‑track silence detection, rotating CDG backgrounds, remote song‑request integration, automatic performance recording and autoplay mode; Vocaluxe is a free open‑source singing game derived from Ultrastar that supports up to six players, reads CDG/MP3+G tracks, scores based on pitch and rhythm and includes party‑mode features; Performous is an open‑source music and dance simulation with a karaoke mode that loads CDG files (MP3+G), offers real‑time pitch detection, scoring, practice tools and visualizations, making all four projects capable of playing CDG karaoke content.

=== mackie-hui-control ===
The Mackie Control HUI (Human User Interface) protocol is a 10‑bit (0‑1023) MIDI‑based control surface specification originally created in 1997 by Mackie and Digidesign for Pro Tools and later adopted by many DAWs (including Logic, Cubase, REAPER, Ableton Live, Ardour and Studio One) and hardware (e.g., SSL UF8, PreSonus FaderPort, Behringer X‑Touch); it defines a fixed set of MIDI messages for motorised fader position, transport (play/stop/record), channel‑strip pan, mute/solo, V‑pot rotary encoders and a 4‑character track name display, typically transmitted on channel 0 with CC 0‑31 for fader values and a series of System‑Exclusive and Note‑On/Off messages for button states. Although Mackie never released the official document and required NDA sign‑offs from licensees, a reverse‑engineered specification has been publicly posted as a PDF (often called “Mackie HUI MIDI control protocol”) and has been used as the basis for several open‑source implementations: the TouchMCU project on GitHub includes a complete markdown description and Arduino‑compatible code for HUI/MCU, an Arduino library for Mackie Control Universal (which also carries HUI support) is available, MusE’s Linux control‑surface module contains a clean‑room re‑implementation, and various community‑maintained fuzzy‑system scripts replicate the fader‑resolution mapping (using the least‑significant‑bit trick for 1024 steps). These resources collectively provide a workable open‑source reference for developers wanting to build or emulate an HUI‑compatible MIDI control surface.

=== midi-2-0-spec ===
The MIDI 2.0 standard defines the Universal MIDI Packet (UMP) as a 32‑bit‑aligned container that can be 32, 64, 96 or 128 bits long, supporting up to 16 independent groups each with 16 channels and carrying both legacy MIDI 1.0 byte‑stream messages and the new MIDI 2.0 voice‑channel messages, with built‑in discovery of UMP endpoints to enable bidirectional switching between protocols; MIDI Capability Inquiry (MIDI‑CI, MA02) provides a universal discovery and configuration layer that uses System Exclusive messages to exchange device capabilities, and its Common Rules for Profiles (MA03, version 1.1, updated 2024) define a schema for Profile specifications that allow devices to auto‑configure behavior via Profile Configuration, Profile Added/Removed, and Profile Details Inquiry messages, while the Common Rules for Property Exchange (MA04, version 1.1, unchanged since 2020) specify a JSON‑based Resource/Property model for getting and setting a wide range of device properties—such as preset names, parameter mappings, and state information—through dedicated MIDI‑CI Property Exchange messages, all of which are required for full UMP‑based operation in the 2025 MIDI 2.0 ecosystem.

=== midi-clock-mtc-sync ===
MIDI Clock and MIDI Time Code (MTC) serve different sync purposes: MIDI Clock is a real‑time, tempo‑dependent stream of 24 pulses per quarter‑note that drives musical timing and is the preferred method for tight beat‑level synchronization in DJ software and hardware, while MTC encodes SMPTE video timecode (hour‑minute‑second‑frame) in eight quarter‑frame messages per frame and does not convey tempo, making it unsuitable for precise musical sync and prone to noticeable jitter because its resolution is limited to about 0.6 ms and the messages can be delayed by other MIDI traffic. To minimise jitter when using MIDI Clock in a DJ setup, keep the clock path simple (avoid daisy‑chaining Thru ports that can create loops), use a dedicated MIDI interface with low‑latency drivers, prioritize real‑time messages in the host’s MIDI queue, and consider locking the host’s internal tempo to an external audio‑derived tempo follower if a live drummer or turntables are the master source; if MTC must be used (e.g., for video‑to‑audio alignment), ensure it is sent on a separate channel, limit simultaneous SysEx traffic, and accept that its timing will be coarser than Clock, using it only for positional reference rather than beat‑accurate triggering.

=== midi-instrument-integration ===
Integrating a MIDI keyboard or drum machine into a live DJ set typically involves sending a MIDI clock from the DJ software (such as Traktor, Serato, or Ableton Live) to the instrument so it follows the software’s tempo, which can be done via a direct MIDI cable from a controller’s MIDI‑out (e.g., a Kontrol S4) or through a MIDI‑enabled mixer that provides beat‑detection and clock output (like the Pioneer DJM‑800/900); alternatively, the instruments can be treated like turntables and manually tempo‑matched, though this offers less precision, while newer solutions use Ableton Link to wirelessly share tempo and phase across iOS apps, computers, and hardware that support the protocol, allowing keyboards, drum machines, and other MIDI controllers to stay perfectly in sync with the DJ software’s beat‑grid and enabling live looping, one‑shot triggering, and real‑time performance without latency‑drift.

=== midi-mapping-learn-ui ===
MIDI‑learn in modern DAWs typically works by entering a dedicated “learn” mode (often via a right‑click on a control or a “MIDI Learn” button) which overlays every parameter with a color‑coded indicator—red for unmapped, green for already mapped, and amber while the system waits for a MIDI message; the user then moves the desired hardware knob, fader or button and the DAW automatically assigns the incoming MIDI CC, pitch‑bend, channel pressure or relative encoder data to that parameter, saves the mapping and optionally provides feedback to motorized controllers, while a “soft‑takeover” or “pickup” mode can be enabled so that the software parameter does not jump to the controller’s current value but instead waits until the hardware position matches the DAW’s existing value before taking control, a feature that most hosts apply to their own tracks, faders and plugin parameters but which requires explicit support inside third‑party plugins to work for their internal knobs.

=== mixxx-architecture ===
Mixxx’s architecture is built around a C++ core that drives audio processing through the Engine, whose central class EngineMixer orchestrates all mixing, beat‑matching and effect routing while the SoundManager handles the audio I/O layer, selecting devices, configuring sample rate and latency and managing the circular sound buffer that supplies the next block of PCM data to EngineMixer; controller support is exposed via a JavaScript/TypeScript‑based scripting API that lets mapping files read and write Mixxx Controls (including a latency‑buffer‑size control) to interact with the engine in real time, and the user interface is being migrated to Qt QML for Mixxx 3.0, replacing the older QtWidgets UI and enabling a modern, easily customizable front‑end while still using the same engine and SoundManager backend.

=== mixxx-controller-scripting ===
Mixxx controller mappings are defined in XML files that specify the controller name, the type (MIDI or HID), and the JavaScript script files to load; each `<controller>` element can include a `<scriptfiles>` list that points to one or more .js files containing QtScript (ECMAScript) functions such as init and shutdown and any custom handlers, and a `<mappings>` section that links individual MIDI or HID messages (identified by status, midino, or HID packet fields) to script functions via a `<key>` tag and a `<Script‑Binding/>` option; the JavaScript API is exposed through a set of declaration files (e.g., engine-api.d.ts, hid-controller-api.d.ts, midi-controller-api.d.ts, console-api.d.ts, color-mapper-api.d.ts) and common helper scripts (common‑controller‑scripts.js, common‑hid‑packet‑parser.js) which are referenced in a jsconfig.json file to enable IDE type checking, and the XML mapping may also contain optional `<settings>` groups for configurable options that the script can read; the controller wizard can generate a basic XML preset for MIDI devices, which you can edit manually to add HID support or more complex behavior, and any additional JavaScript files must be placed alongside the XML in the user controller mapping folder and listed exactly as they appear in the `<scriptfiles>` section.

=== mixxx-features ===
Mixxx 2.5 is a free, cross‑platform DJ application that supports two‑ or four‑deck layouts with customizable skins, full‑size waveform displays, beat‑grid and key detection, automatic BPM and musical‑key analysis, sync‑lock and leader‑deck selection, hot‑cues, loop creation, beat‑roll and reverse‑censor effects, quantized cue‑point and loop placement, an Auto‑DJ mode with cross‑fader automation, extensive MIDI/HID controller mapping (including built‑in mappings for popular decks), vinyl‑timecode control and DVS support, a built‑in sampler, microphone and auxiliary inputs, ReplayGain loudness normalization, library integration with iTunes, Traktor, Banshee, Rekordbox, Rhythmbox and Serato, broad audio format compatibility (FLAC, WAV, AIFF, MP3, M4A/AAC, Ogg Vorbis, Opus and WV), a full suite of built‑in audio effects that can be chained and knob‑controlled, recording to lossless WAV/FLAC or lossy Ogg Vorbis (MP3 via LAME) with automatic archiving, live broadcasting to Shoutcast or Icecast servers, and four selectable skins; real‑time stem‑mixing is not included in the stable 2.5.1 release but was added later in the 2.6 beta, extending the Native Instruments stem specification to support additional codecs.

=== mopidy-snapcast ===
Mopidy‑Party is an open‑source web client extension that adds a “party mode” interface for controlling Mopidy from a browser, allowing users to queue and manage tracks collaboratively; Mopidy‑MPD is the official Mopidy extension that implements the Music Player Daemon protocol, exposing Mopidy’s playback engine to any MPD‑compatible client and offering configurable host, port, and password settings; Snapcast is an open‑source synchronous multi‑room audio system that turns any audio source into a time‑aligned network stream by feeding PCM data (often via a FIFO such as /tmp/snapfifo) from Mopidy to a Snapcast server, which then distributes the stream to Snapcast clients on each speaker device, enabling perfectly synced playback across rooms.

=== music-metadata-apis ===
MusicBrainz offers a free, open‑source REST API that returns canonical music‑metadata such as artists, releases, recordings, and their unique MBIDs in JSON or XML without OAuth (though it enforces rate limits and encourages proper user‑agent strings); Discogs provides a commercial API that returns both community‑generated release metadata and marketplace data (including images, formats, and pricing) but requires OAuth authentication, stricter rate limits, and approval for commercial use; AcoustID supplies a fingerprint‑lookup service that, given a Chromaprint fingerprint, returns an AcoustID identifier plus any linked MusicBrainz recording MBIDs and optional user‑submitted textual tags, accessed via a simple API key without OAuth; Spotify’s Web API delivers both rich catalog metadata and detailed audio‑feature analysis (acousticness, danceability, energy, instrumentalness, liveness, loudness, speechiness, tempo, time‑signature, etc.) for tracks identified by Spotify IDs, but requires an OAuth token, enforces commercial‑use approval, and is rate‑limited, making it the only service among the four that provides proprietary acoustic descriptors while the others focus on community‑curated metadata and identifier resolution.

=== music-streaming-dj-api ===
Beatport LINK and Beatsource LINK are both built‑in streaming options on Denon’s Engine OS hardware (Prime 4, Prime Go+, SC Live 2/4, Mixstream Pro +, etc.) and are also supported as official integrations in major DJ software such as Serato DJ Pro/Lite, Rekordbox, VirtualDJ, Traktor Pro 3, DJUCED, djay Pro (desktop and iOS), and Engine DJ OS, allowing DJs to stream full‑track, high‑quality audio directly from the catalog without needing local downloads; Tidal is likewise integrated across those same platforms, offering Hi‑Fi lossless streams where the DJ software supports it. SoundCloud Go+ is available in djay Pro, Serato, Rekordbox and other leading apps, but developers must obey SoundCloud’s API Terms of Use, which require a registered app with a client ID, enforce usage quotas, and prohibit accessing Go‑only content unless the developer has a separate written agreement with SoundCloud that includes robust authentication; the API also mandates proper attribution (crediting the uploader, SoundCloud and providing permalink backlinks) and forbids circumventing any imposed limits. Beatport’s streaming service (Beatport Streaming) now lists a wide range of DJ integrations—including CDJ‑3000/X, DEX, djay Pro, DJ Studio, DJUCED, Engine OS, future.dj.pro, OMNIS‑DUO, OPUS‑QUAD, rekordbox, Serato, Traktor, Tribe XR, VirtualDJ, WeDJ and XDJ‑AZ—offering unlimited full‑track playback and, where supported, real‑time stem separation. Beatsource is being merged into Beatport and while legacy Beatsource subscriptions still work, new users are directed to Beatport LINK for open‑format music. All of these services require an active subscription and, where applicable, a stable Wi‑Fi connection on the hardware or computer running the DJ software.

=== navidrome-subsonic ===
Navidrome is an open‑source, self‑hosted music streaming server that indexes your local collection and makes it accessible via a modern web UI as well as any Subsonic‑compatible client, offering full Subsonic API support for tag‑based browsing, folder‑style navigation, playback, transcoding, jukebox mode, bookmarks, star ratings, scrobbling to Last.fm, ListenBrainz and Maloja, and public sharing links. It provides comprehensive playlist capabilities, including import and synchronization of .m3u files, creation and editing of regular playlists, and smart or dynamic playlists that automatically update based on tag criteria similar to iTunes, all while supporting on‑the‑fly transcoding (including Opus), multi‑user accounts, external authentication, and integration with numerous mobile and desktop apps that speak the Subsonic API.

=== open-source-dj-software ===
Open‑source DJ applications include Mixxx — a full‑featured, cross‑platform DJ suite with beat‑matching, key detection and extensive controller support; xwax — a minimalist, low‑latency DVS‑oriented player designed for vinyl‑style timecode control; Ultramixer — a lightweight Linux‑only mixer that provides basic deck functions and MIDI mapping; and a growing collection of community‑maintained projects on GitHub such as the Mixxx‑dj repository and smaller tools like the “dj‑engine” and “libre‑dj” libraries that offer modular playback, cueing and effects for developers building custom DJ solutions.

=== open-source-jukebox ===
CrownJukebox (github.com/Kronborgs/crownjukebox) is a self‑hosted, open‑source party jukebox built with Go, React and Docker that lets multiple users add tracks from their phones to a shared, real‑time queue without any cloud services, includes a “SKÅL!” party mode that triggers a full‑screen neon animation and a dedicated party playlist, and provides granular access controls (search, view queue, add to queue, use party button) plus an admin panel for managing the library and permissions; other notable self‑hosted options include Jukebox.Local, a collaborative local‑network jukebox that runs on Raspberry Pi and supports crowd queuing of personal music collections, Hangton‑Code’s event‑music‑system which turns a projector into a QR‑based YouTube jukebox with optional AI filtering, and Count Jukeula (juke‑rs) which offers a web interface for Spotify‑backed collaborative queuing, but CrownJukebox most directly matches the request for an open‑source, Docker‑deployable, retro‑styled jukebox with real‑time crowd queue and party features.

=== osc-dmx-lighting ===
Open Sound Control (OSC) can be combined with DMX‑based lighting via several free, open‑source projects that are well suited for DJ‑style light shows: the Open Lighting Architecture (OLA) provides a robust OSC‑to‑DMX/Art‑Net bridge and includes a C++ library (libola) plus a Python API for mapping OSC messages to DMX universes; liblo is a lightweight C library that handles OSC packet parsing and can be paired with OLA or with custom code to translate OSC to Art‑Net. For ready‑made applications, LightGroove is a modern web‑based DMX controller that runs on Windows, macOS and Linux, offers Art‑Net output, BPM‑sync and a browser UI that any DJ can access from a phone or laptop; DigiShow is a Qt‑based show‑control app written in C++/QML that ships with TinyOSC, RtMidi and built‑in Art‑Net support, allowing you to map MIDI, OSC, DMX and other protocols in a single patch and is explicitly marketed for DJ performances; QLC+ (Q Light Controller Plus) and DMXControl 3 are also open‑source DMX suites with plug‑ins for OSC input and Art‑Net output, making them viable choices for integrating music software, VJ tools or iPad controllers (e.g., Lemur) into a synchronized light rig. All of these libraries and applications can run on typical DJ hardware and provide the OSC‑to‑DMX/Art‑Net conversion needed to drive professional fixtures from a DJ’s music tempo.

=== raveberry-partyplay ===
Raveberry is an open‑source, Raspberry Pi‑focused multi‑user music server that lets party guests request and vote on songs from YouTube, Spotify, SoundCloud and local files through a web UI, and its LGPL‑3.0‑licensed code is hosted on GitHub with hundreds of stars; Festify is a free, browser‑based Spotify‑powered jukebox where participants join via a short URL, vote to push tracks up the queue, and the host (who must have Spotify Premium) can use an admin mode to skip, delete or pause songs; comparable GitHub projects include the copykatze/jukebox repo that implements a similar democratic selection model, the PartyPlay Node.js/React app that uses Spotify’s Web Playback SDK for voting‑based queues, and Mixody, a web‑app offering event‑focused voting and host‑relief features as a non‑Spotify‑specific alternative, all of which can be self‑hosted and customized to create a participatory party music jukebox.

=== rekordbox-features ===
Pioneer’s rekordbox DJ performance mode includes two key subscription‑based features: CloudDirectPlay and Lighting mode. CloudDirectPlay, launched with firmware 2.0 for the CDJ‑3000, lets a DJ log into their rekordbox account on the player and stream their entire cloud‑based library directly from the hardware without needing a USB drive, preserving all metadata such as Hot Cues and Memory Cues and automatically syncing any edits back to the cloud; this capability is available to users of the rekordbox Creative and Professional subscription plans, which start at $14.99 per month and also provide Cloud Library Sync. Lighting mode, which automatically generates lighting cues based on track phrases and allows full DMX‑512 control via the optional RB‑DMX1 interface, is bundled with the Professional plan, enabling DJs to design, save and trigger synchronized light shows directly from rekordbox.

=== rtmidi-libremidi ===
RtMidi is a lightweight, header‑only C++ library that provides a simple object‑oriented API for real‑time MIDI input and output on Linux (ALSA and JACK), macOS (CoreMIDI and JACK), Windows (Multimedia and UWP), iOS, Android and WebMIDI; it is released under a permissive BSD‑style license, focuses on minimal dependencies, enumerates devices by numeric IDs and historically lacked built‑in MIDI 2 support, but its straightforward design makes it easy to embed in small projects. libremidi is a modern rewrite derived from RtMidi and ModernMIDI that targets C++20, adds comprehensive MIDI 2 handling on macOS (11+), Linux (kernel 6.5+), and Windows 11 (or Windows 10 with the new MIDI Services), and introduces features such as hot‑plug aware observers, stable handle‑based port identification, reduced virtual‑function overhead, optional static‑ or small‑vector message storage for hard‑real‑time use, nanosecond timestamps, and full WebMIDI support; it is also open source (MIT‑like) and ships with extensive documentation and a modular CMake build. JUCE is a full‑featured, cross‑platform C++ framework that includes a robust MIDI module offering classes for MIDI message handling, device enumeration, virtual ports, MIDI file I/O, and integration with its audio and GUI subsystems; JUCE’s MIDI support covers CoreMIDI, ALSA, JACK, and Windows MM/UM, works on desktop, iOS, Android and WebAssembly, is released under the GPLv3 with a commercial license option, and while it does not yet provide native MIDI 2 APIs, its high‑level abstractions, real‑time safety, and tight coupling with audio processing make it the most comprehensive solution for applications that already use JUCE’s broader toolset. In summary, RtMidi is the most minimal and widely compatible option for basic MIDI 1 tasks; libremidi builds on that foundation with modern C++20 features, MIDI 2 support, and performance optimizations; and JUCE offers the richest ecosystem and higher‑level MIDI utilities at the cost of a larger framework and a dual‑licensing model.

=== serato-dj-pro-features ===
Serato DJ Pro’s 2025 feature set includes full Stems support that lets you isolate and remix vocals, instruments, bass and drums on‑the‑fly and apply Stems Pad FX such as Echoes and Brakers, a Practice Mode that lets you mix two or more tracks without any hardware connected, native DVS compatibility for turntables and CDJs (enabled through the optional DVS Expansion Pack), and a suite of expansion packs that add specialized functionality – the DVS Expansion Pack for turntable control, Serato Video for synchronized video playback and VJ‑style effects, Serato Play for advanced laptop‑only DJing, plus additional packs like the FX Pack, Remix Pack and other genre‑specific add‑ons; the core software also offers 4‑deck mixing, up to 50 built‑in FX, Sampler slots, Beat Jump, Slicer, Quantize, Smart Sync, Key Analysis, and MIDI mapping, giving professional DJs a comprehensive toolbox for creative performance.

=== stem-separation-open-source ===
Spleeter is the fastest open‑source stem separator: on a modern GPU it runs at roughly 100 × real‑time, meaning a three‑minute track can be processed in about two seconds and the model only needs a 2–3 second look‑ahead buffer, giving sub‑second to low‑single‑digit‑second latency that makes real‑time or live‑performance use feasible; it also runs acceptably on CPUs at a few‑times‑real‑time speed. Demucs, by contrast, prioritises quality over speed: even with a recent GPU (e.g., RTX 3080 or RTX 4060) it processes a three‑minute song in roughly 20–30 seconds (5–10 × real‑time), and on CPU it can be slower than real‑time (≈0.8 × real‑time on a high‑end desktop), resulting in latency on the order of tens of seconds, and it typically requires 4–8 GB of VRAM and higher memory usage, so it is unsuitable for true real‑time applications but delivers markedly cleaner stems.

=== timestretch-open-source ===
Rubber Band is an open‑source audio time‑stretching and pitch‑shifting library released under the GNU GPL (v2 or later), which means you can use and modify it freely in GPL‑compatible projects but must distribute any derived work under the same license; for proprietary or App‑Store distribution a commercial licence (often sold for a few hundred dollars for unlimited use) is required. SoundTouch is also open source but uses a permissive license (LGPL‑2.1 with a static‑link exception that effectively allows commercial use without open‑sourcing your code, provided you honor the attribution and source‑availability requirements for the library itself). Elastique, by Z‑Plane, is not open source at all—it is a proprietary, commercial‑only product with a relatively high licensing cost and no free version, so it can only be used under a paid licence.

=== touchtunes-app-features ===
The TouchTunes app lets users locate nearby bars, restaurants and other venues on an interactive map, browse the full jukebox queue in real time and add songs to play, while offering a “Fast Pass” option that lets you spend credits to jump ahead and hear your track sooner; it also provides a song‑upgrade feature, displays each song’s position in line, and includes a loyalty program (TouchTunes Rewards) that awards points and free credits for each paid play, lets you earn perks and redeem them for future songs.

=== touchtunes-business-model ===
TouchTunes installs its digital jukeboxes in bars, restaurants and nightclubs at no upfront cost to the venue, then collects a per‑song fee that includes the public‑performance licensing payments it has negotiated with record labels, music publishers and PROs such as ASCAP, BMI and SESAC; after deducting those licensing costs and any equipment‑service charges, the remaining revenue is split between the venue owner and the local amusement‑machine operator, typically with the venue receiving roughly a quarter to one‑half of the net proceeds (often described as a fixed 24 % of gross revenue after a small monthly reimbursement) and the operator retaining the balance, while TouchTunes retains the song‑fee revenue and benefits from the licensing agreements.

=== touchtunes-overview ===
TouchTunes turns a bar’s digital jukebox into a phone‑controlled music player: users open the TouchTunes app, use the built‑in map to locate a nearby venue, and then browse its full catalog of millions of licensed songs, add tracks to the venue’s queue, and see their exact position in real time; purchased credits— which never expire and can be stored in a wallet or auto‑refilled—pay for each song, while “Fast Pass” or “Song Upgrade” lets a patron spend extra credits to move a selected track ahead of the line and play it next, and the app also shows the entire queue so users can watch who played what, create personal playlists, earn loyalty points for free credits, react with emojis, and quickly favorite or search songs, all from a single mobile interface that lets you be the DJ in any participating bar or restaurant.

=== touchtunes-technology ===
TouchTunes’ current hardware line—Playdium, Virtuo (and Virtuo II) and Angelina—are all built as modular, service‑friendly digital jukeboxes that rely on a cloud‑based platform for music catalogs, real‑time analytics, remote updates and venue‑operator management; Playdium’s design includes interchangeable field‑replaceable units such as backplane, payment‑door and camera modules and is wired for broadband or wireless Ethernet via CAT‑5 or Wi‑Fi, Virtuo and Virtuo II feature a snap‑in part architecture with a removable motherboard, HD 24‑ or 27‑inch touchscreens, adjustable LED lighting and optional photobooth or karaoke add‑ons, while Angelina takes the modular concept further with a removable motherboard and integrated lighting and visualizer systems, all three models connecting to TouchTunes’ cloud services to stream licensed music, push software and lighting updates, and deliver usage data to operators.

=== traktor-pro-features ===
Native Instruments Traktor Pro 4 is a four‑deck DJ platform that combines traditional mixing tools with advanced creative features: it includes Remix Decks that let you load and trigger samples, loops and stem files on any deck, and a built‑in Pattern Player that provides up to four independent drum‑machine units with 17 kits and step‑sequencing for polyrhythmic patterns; the software also offers AI‑powered stem separation (iZotope RX) with independent volume, filter and FX control for each stem, flexible beat‑grid detection and editing, tempo and beat sync modes, 43 built‑in effects across nine mixer/channel slots, Auto‑Master, key‑lock, hot‑cues/loops, DVS timecode support and full integration with external MIDI, Ableton Link and compatible controllers.

=== virtualdj-features ===
VirtualDJ 2026 adds powerful real‑time stem separation that instantly isolates vocals, instruments, drums, bass and hi‑hats on any track during a mix, with dedicated performance pads and a ModernEQ for precise control; it introduces a fully featured Sandbox Mode that lets DJs jump ahead in the set without interrupting the audience and automatically restores the original state when disengaged; video mixing is expanded with AI‑generated visual loops, shaders and beat‑synced video loops that can be prompted by text, allowing audio‑only tracks to be paired with dynamic visuals; and the sampler is upgraded to a StemSwap sampler that records stem samples on the fly and plays them back with automatic stem replacement, supporting per‑song banks, paginated pads and instant on‑the‑fly mash‑ups and remixes.

=== web-audio-dj-open-source ===
Several open‑source, browser‑based DJ mixers built with the Web Audio API are available on GitHub, the most prominent being DJ23, a casual two‑deck DJ mixer that runs entirely in the browser and uses the Web Audio API for low‑latency playback, beat‑matching, and optional Web‑MIDI controller support; GridSound, a work‑in‑progress free digital audio workstation that includes multitrack mixing, effects, and a full Web Audio‑API backend; Waveform‑Playlist, a multitrack Web Audio editor and player that displays waveforms on a canvas, lets users set cues, fades and shift tracks in time, and can export mixes to AudioBuffer or WAV; and a lightweight “web‑audio‑mixer” repository that demonstrates basic deck cross‑fading, volume control and synchronisation using Web Audio nodes. All of these projects are licensed under permissive open‑source terms, include source code and documentation on GitHub, and can be run directly in any modern browser without additional plugins.

=== web-midi-api ===
Chrome has supported the Web MIDI API on desktop and Android since version 43, but it only works in a secure (HTTPS) context and, as of Chrome 124, the entire API is gated behind a user‑permission prompt; a separate SysEx grant is still required when calling navigator.requestMIDIAccess({sysex:true}) and the user must approve that dialog. The API’s basic MIDI 1.0 functions (sending and receiving Uint8Array messages, onstatechange events, timestamps, etc.) are fully enabled by default, while MIDI 2.0 Universal MIDI Packet (UMP) handling is only available experimentally in recent Chrome builds (behind the “Enable MIDI 2.0” flag) and is not yet enabled in stable releases.


